{
  "private": true,
  "version": "0.1.0",
  "description": "Ready to use template project for benchmarking Purescript and JS",
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build --censor-lib --strict",
    "bundle": "rollup --config"
  },
  "dependencies": {
    "benchmark": "^2.1.4",
    "lodash": "^4.0.0",
    "microtime": "^2.1.3",
    "rollup": "^0.41.6",
    "rollup-plugin-copy": "^0.2.3",
    "rollup-plugin-purs": "^1.0.37"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript-psa": "^0.5.0-rc.1",
    "rimraf": "^2.5.0"
  }
}
